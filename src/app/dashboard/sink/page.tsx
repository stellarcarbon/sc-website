"use client";

import CheckoutForm from "@/containers/CheckoutForm";

export default function DashboardSink() {
  return (
    <div>
      <CheckoutForm doCheckoutFlow={() => {}} />
    </div>
  );
}
